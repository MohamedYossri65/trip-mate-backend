export enum ConversationType {
  DIRECT = 'DIRECT',
}

export enum MessageType {
  TEXT = 'TEXT',
  ATTACHMENT = 'ATTACHMENT',
  IMAGE = 'IMAGE',
  PDF = 'PDF',
  AUDIO = 'AUDIO',
  OFFER = 'OFFER',
}
